# サッカートーナメント管理アプリ プロジェクト引き継ぎ資料

## プロジェクト概要
このプロジェクトは、中野区シニアサッカー大会などのトーナメントやリーグ戦を管理するためのWebアプリケーションです。
ReactとViteを使用して構築されており、バックエンド（データベースおよびホスティング）にはFirebaseを利用しています。

## 技術スタック
- **フロントエンド**: React, Vite, Lucide React (アイコン)
- **スタイリング**: Vanilla CSS (`src/index.css` に集約、グラスモーフィズムデザインを採用)
- **バックエンド**: Firebase Realtime Database
- **ホスティング**: Firebase Hosting / GitHub Pages (設定による)
- **主な機能**: 
  - 試合スケジュール・結果の管理と表示
  - 順位表、得点王、アシスト王の自動集計
  - チームごとの登録メンバー管理（追加、修正、削除、出欠・チェックモード）
  - 記録用紙（PDF）の生成・印刷ビュー
  - ルール表示などの動的切り替え

## ディレクトリ構成と主要ファイル
- `src/App.jsx`: アプリケーションのすべてのコアロジック、UIコンポーネント、Firebaseとの通信処理がこの1つのファイルに集約されています。改修を行う際は主にこのファイルを編集します。
- `src/index.css`: アプリケーション全体に適用されるCSSスタイル。レスポンシブ対応やUIのカラーテーマ（ダークモードベース）が含まれています。
- `scratch/`: 過去に使用した初期データ投入スクリプトや、Googleスプレッドシートからのメンバーインポートスクリプト（`parse_xlsx.js`, `merge_and_upload.js`など）が含まれています。

## 直近の実装・修正内容 (2026年7月時点)
- **メンバー管理機能の刷新**: メンバー一覧の「追加」「修正」操作をUI上部に統一し、編集中は対象行を最上部に移動させる仕様に変更。また、誤操作防止のための「削除モード」を実装。
- **データインポート**: Googleスプレッドシートから「FCネロ」などのメンバーリストを抽出し、Firebaseへマージして取り込みました。
- **オウンゴール対応**: 得点者の選択肢に「オウンゴール」を追加し、個人の得点王ランキングの集計からは除外（チームの得点としては加算）するロジックを実装。
- **UI調整**: スマホ表示時の入力フォーム見切れ防止、各種ボタンのアイコン統一（❌など）、バッジの簡略化。

## Claude Code への引き継ぎ手順
Claude Codeを起動した際、この `HANDOVER.md` を読み込ませることで、これまでの文脈やシステム構成を瞬時に理解させることができます。

**Claude Codeへのプロンプト例:**
> 「このディレクトリにある `HANDOVER.md` を読んで、プロジェクトの構成と現在の状況を把握してください。その後、以下の機能追加（または修正）をお願いします：〇〇〇〇」

## 開発環境の起動方法
```bash
# 依存関係のインストール (初回のみ)
npm install

# ローカル開発サーバーの起動
npm run dev

# プロジェクトのビルド
npm run build
```
